{"id":1278821384,"question_id":84,"lang":"cpp","lang_name":"C++","time":"1 month","timestamp":1717616574,"status":20,"status_display":"Compile Error","runtime":"N/A","url":"/submissions/detail/1278821384/","is_pending":"Not Pending","title":"Largest Rectangle in Histogram","memory":"N/A","code":"class Solution {\npublic:\n    int largestRectangleArea(vector<int>& heights) {\n        // sort(heights.begin(),heights.end(),greater<int>());\n        stack<int> st;\n        int minVal , maxVal = 0;\n        for(auto h:heights){\n            if(st.empty()){\n                minVal = h;\n                st.push(h);\n                maxVal = max(maxVal , int(st.size() * minVal));\n            }else {\n                if(h > st.top()){\n                    minVal = st.top();\n                    st=stack<int>();\n                    st.push(minVal);\n                    st.push(h);\n                    maxVal = max(maxVal , int(st.size() * minVal));\n                else if (h == 0){\n                    st=stack<int>();\n                }else {\n                    st.push(h);\n                    minVal = min(h,minVal);\n                    maxVal = max(maxVal , int(st.size() * minVal));\n                }\n            }\n        }\n        return maxVal;\n    }\n};","compare_result":null,"title_slug":"largest-rectangle-in-histogram","has_notes":false,"flag_type":1}